export const environment = {
  BACK_URL: 'http://localhost:4000/api',
};

//Se cambia de todos losarchivos de servicios private baseUrl
//Si se cambia el puerto AQUI se cambia automaticamente en todos los archivos
